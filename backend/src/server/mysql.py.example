

# [!SENSITIVE!] Name of the table in INVENTORY_DB_NAME database to store the
#               Inventory users
# [!SETUP!] This needs to be changed before first use and match the variable of 
#           the same name in the backend/.env file
MYSQL_USER = 'inventory_user'

# [!SENSITIVE!] Name of the table in INVENTORY_DB_NAME database to store the
#               Inventory users
# [!SETUP!] This needs to be changed before first use and match the variable of 
#           the same name in the backend/.env file
MYSQL_PASSWORD = 'inventory24'