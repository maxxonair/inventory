[supervisord]
nodaemon=true

[program:mariadb]
command=/usr/local/bin/docker-entrypoint.sh mysqld
autostart=true
autorestart=true
priority=10

[program:inventoryserver]
command=/root/.local/bin/uv run -m server.InventoryServer
directory=/app
autostart=true
autorestart=true
priority=20
